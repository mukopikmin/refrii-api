paths:
  /users:
    get:
      tags:
        - Users
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
            image/*:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
    post:
      tags:
        - Users
      requestBody:
        $ref: "#/components/requestBodies/postUsers"
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
            image/*:
              schema:
                $ref: "#/components/schemas/User"
  /users/{id}:
    get:
      tags:
        - Users
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
            image/*:
              schema:
                $ref: "#/components/schemas/User"
    put:
      tags:
        - Users
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        $ref: "#/components/requestBodies/postUsers"
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
            image/*:
              schema:
                $ref: "#/components/schemas/User"
  /users/{id}/push_token:
    post:
      tags:
        - Users
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
            image/*:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: erroe