paths:
  /users:
    get:
      tags:
      - Users
      responses:
        200:
          schema:
            type: array
            items:
              $ref: "#/definitions/User"
    post:
      tags:
      - Users
      parameters:
      - in: formData
        name: email
        type: string
        required: true
      responses:
        204:
          schema:
            $ref: "#/definitions/User"
  /users/{id}:
    get:
      tags:
      - Users
      parameters:
      - in: path
        name: id
        type: integer
        required: true
      responses:
        200:
          schema:
            $ref: "#/definitions/User"
    put:
      tags:
      - Users
      parameters:
      - in: formData
        name: email
        type: string
        required: true
      responses:
        200:
          schema:
            $ref: "#/definitions/User"
  /users/{id}/push_token:
    post:
      tags:
      - Users
      parameters:
      - in: path
        name: id
        type: integer
        required: true
      responses:
        201:
          schema:
            $ref: "#/definitions/User"