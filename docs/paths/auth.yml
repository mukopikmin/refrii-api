paths:
  /auth:
    post:
      tags:
        - Authentication
      responses:
        "200":
          description: Success authorization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Credential"
            image/*:
              schema:
                $ref: "#/components/schemas/Credential"
              

