paths:
  /auth:
    post:
      tags:
      - Authentication
      responses:
        200:
          description: Success authorization
          schema:
            $ref: "#/definitions/Credential"
              

