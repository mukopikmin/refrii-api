definitions:
  Credential:
    type: object
    additionalProperties: false
    properties:
      jwt:
        type: string
      expires_at:
        type: string
        format: date-time
      user:
        $ref: "#/definitions/User"
    required:
    - jwt
    - expires_at
    - user