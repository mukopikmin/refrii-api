definitions:
  Invitation:
    type: object
    additionalProperties: false
    properties:
      id:
        type: integer
      box_id:
        type: integer
      user_id:
        type: integer
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time
    required:
    - id
    - box_id
    - user_id
    - created_at
    - updated_at