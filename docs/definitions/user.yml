definitions:
  User:
    type: object
    additionalProperties: false
    properties:
      id:
        type: integer
      name:
        type: string
      email:
        type: string
      admin:
        type: boolean
      disabled:
        type: boolean
      provider:
        type: string
      avatar_url:
        oneOf:
        - type: string
        - type: null
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time
    required:
    - id
    - name
    - email
    - admin
    - disabled
    - provider
    - avatar_url
    - created_at
    - updated_at