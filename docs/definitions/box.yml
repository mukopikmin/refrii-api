definitions:
  Box:
    type: object
    additionalProperties: false
    properties:
      id:
        type: integer
      name:
        type: string
      notice:
        type: string
      is_invited:
        type: boolean
      image_url:
        oneOf:
        - type: string
        - type: null
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time
      owner:  
        $ref: "#/definitions/User"
      invited_users:
        type: array
        items:
          $ref: "#/definitions/User"
    required:
    - id
    - name
    - notice
    - is_invited
    - image_url
    - created_at
    - updated_at
    - owner
    - invited_users